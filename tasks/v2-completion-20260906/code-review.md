# Independent completion source review

Review status: **APPROVE FOR SOURCE REVIEW — no outstanding source findings in the latest archive projection repair.** Earlier findings are resolved. Individual sections retain their historical verification boundaries. This is a source review; it does not certify visual quality, measured performance, deployment, or founder acceptance.

Scope: local unstaged changes against `415dc4368b44efc06d0d28928362901f9624a20f`; staged diff was empty. Reviewed `mascot.js`, `mascot-loader.js`, `mascot.css`, `skyy-mascot.php`, related concierge harness/tests, `inc/performance.php`, and the native cart override/contract test. Read unchanged `skyy-3d.js` event handlers to check the new presence layer against actual renderer ownership. Also inspected the relevant theme CSS and enqueue changes. No PR was supplied, so PR merge readiness/CI was not available and is not claimed.

## Finding

**RESOLVED — match Woo's stronger disabled alternate-button states.** The original theme disabled selector had specificity `(0,4,1)`, below native attribute/hover combinations. Independently verified the added `.alt:is(disabled-states):hover`, `.alt:disabled[disabled]`, and `.alt:disabled[disabled]:hover` selectors in the same disabled-token rule group. They match the native combinations through `(0,6,1)`, allowing the later theme rules to retain disabled colors. Base and enabled-hover selectors likewise match native specificity. Compiled output and actual computed states, including hover with an explicit disabled attribute, still require the planned build/browser check; this reviewer did not run either.

Additional latest-delta scope: the Home stage now reserves an 8rem controls column, avoiding dependence on the late Pause button's intrinsic width; body Woo classes own Cart/Checkout outer padding including notices; Search heading is capped at 4.5rem with existing all-width wrapping. No additional source defect found in these changes. Actual geometry and GPU transition evidence remain with the browser reviewer.

**RESOLVED — retain announcement behavior when Woo inserts a populated error notice.** The original high finding concerned replacing native `role="alert"` with a generic populated live region, which loses the special insertion behavior documented in [MDN live-region guidance](https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Guides/Live_regions). Independently verified the corrected structure: an outer `div.woocommerce-error[role="alert"]` containing `ul.sr2-notice-list[role="list"]`, with no redundant `aria-live`. Native notice data and escaping calls remain unchanged. Installed native cart/checkout selectors match the outer class without requiring a UL, preserving whole-notice selection/removal. Inner-list margin/padding reset is scoped to this notice. The updated PHP contract passes and requires both containers. This closes the source defect; its “live announcements” output still denotes markup-contract verification rather than an observed screen-reader session. Dynamic insertion and axe checks remain assigned to the browser reviewer.

Final-delta review otherwise found the native data-attribute/notice-escaping calls intact and no actionable issue in mobile About grid/min-width/heading wrapping or all-viewport Search wrapping. The harness now uses the supplied longer unmatched query. The updated PHP cart/notice contract passed independently; browser/axe checks remain the separate lane.

**RESOLVED — localize the new guide-load failure label and title.** The original medium finding concerned hardcoded English at `wordpress-theme/skyyrose-flagship-2/assets/js/mascot-loader.js:120–125`. The owner now reads translated `data-guide-failed` and `data-contact` values emitted with `esc_attr__()` in the server template. Assignments remain text-only; the native Contact URL and focus handoff survive. A Spanish-label regression test verifies the failure branch. Independently reread this delta and reran the focused tests; no remaining finding here.

Additional follow-up scope: reviewed replacement of undefined `--sr2-space-7` references with the existing spacing token, the source-spacing resolution regression test, outer checkout-page padding covering notices before the form, and generic-heading padding consolidation. The token test catches unresolved numeric spacing references without CSS fallback; it makes no claim about every CSS custom property. No additional actionable defect found in those runtime deltas.

**RESOLVED — preserve each engine's final responsive receipt.** The originally added harness always targeted `completion-responsive.json`, allowing sequential engines in the same directory to overwrite the first final receipt. Independently verified the corrected module-entry allowlist (`chromium`/`webkit`) and engine-qualified `completion-responsive-${engineName}.json` target. The receipt helper derives its sidecar from that target, so both final receipt and sidecar now remain separate for each engine. Repeated `node --check` passed. This finding concerned evidence retention, not storefront runtime behavior.

The responsive harness was independently read with its runtime and receipt helpers, and `node --check` passed. It confines requests to the configured loopback origin, checks all declared row counts and response statuses, records unhandled page exceptions and HTTP resource errors, and writes failure receipts when an assertion rejects. Reduced motion and disabled screenshot animations are appropriate for its static reflow scope. Its current measurements concern initial-viewport document reflow and axe results at 390/1440; they do not certify below-fold lazy-image completion, console-message errors, clipping hidden by overflow rules, full interaction states, or performance. Those require the other stated evidence lanes. No browser was launched by this reviewer.

## Verified behavior and limits

- The presence transition restores the existing portrait instead of introducing duplicate nodes or a second renderer. Cancellation covers fallback, hide, and page lifecycle events. The renderer's documented visibility event is observed before the canvas opacity changes. Repeated visible events restore the existing portrait display without starting another transition when already live.
- Loading/fallback text does not disable the deterministic guide. Text answers remain text nodes, and output links/scripts retain same-origin HTTP(S) validation. No additional network service, commerce mutation, model change, or paid call was introduced in this reviewed patch.
- Native Woo styles are removed only inside the existing governed-route guard and the narrower Home/current collection predicate, with an extension opt-in filter. Native transactional and archive CSS remains outside that removal. This source review cannot prove the appearance of every extension's markup; current theme-owned bag, notice, and Quick View appearance remains a browser gate.
- New cart prices/subtotals use native Woo methods and filters; quantity output preserves the trusted native extension HTML contract rather than stripping form inputs. Product-derived remove-link fields are escaped. Backorder and cart coupon hooks align with installed Woo template behavior. Real checkout/payment outcomes are outside this review.
- Crossfade timing, the portrait's 128% painted scale, landscape layout, assistive-technology announcement quality, and actual GPU frame output require the independent browser/pixel lane and are not approved from source alone.

## Checks run

- Initial `node --test tools/v2-runtime/test-skyy-concierge.cjs`: **23 passed, 0 failed**. Follow-up `node --test tools/v2-runtime/test-skyy-concierge.cjs tools/v2-runtime/test-token-system.cjs`: **32 passed, 0 failed**, including the new translated fallback and spacing-reference regressions.
- `php wordpress-theme/skyyrose-flagship-2/scripts/test-performance.php`: **passed**.
- `php tools/v2-runtime/test-cart-contract.php`: **passed**.
- TypeScript check skipped: this scoped theme change is JavaScript-only; its package has no TypeScript check/configuration.
- ESLint unavailable for this scope: no theme ESLint dependency/configuration/script, no theme/root installed ESLint executable, and no executable on PATH. The root lint script targets unrelated `src` files. No dependency installation or synthetic lint configuration was introduced to manufacture a check.
- Tests emitted Node environment/module-type warnings; no test failure. No browser, build, deployment, source edit, or commit performed by this reviewer.

## Performance-target follow-up against aabd2bff

Established the new local unstaged scope against current HEAD `aabd2bffdc1e322862acd05a5640c14cf00f3acf`: `inc/performance.php` and `scripts/test-performance.php`. This is a separate follow-up from the earlier `415dc4368` completion review. **No actionable finding in the bounded native inline-style delta.**

The new hook assigns only Core `path` metadata to explicitly named queued theme handles whose registered URL exactly equals the corresponding readable local CSS path. Each candidate is bounded at 16 KiB. Debug mode, deployment opt-out, alternative media, RTL replacement, conditional style, an existing path override, nonselected handles, and replacement URLs preserve ordinary delivery. It adds no JavaScript dependency or execution-strategy change and does not enlarge Core's total inline budget.

Read the installed Core `wp_maybe_inline_styles()` implementation and hook registration to verify the dependency: Core retains its 40,000-byte aggregate selection budget, normalizes relative CSS URLs, inserts the selected file before existing attached `after` CSS, and leaves handles/order to native style output. The new theme code does not emit its own style tags or hide content pending script execution. This source inspection supports the intended no-JavaScript path; real rendered output and performance remain browser gates.

Independently ran `php wordpress-theme/skyyrose-flagship-2/scripts/test-performance.php`: **passed**. Tests cover intended handle/path registration, attached extension CSS preservation, media/RTL/nonselected/plugin exclusions, replaced URL, deployment opt-out, and absence of a budget-expansion filter. No build, HTTP, browser, or container actions performed for this recheck. The separately repaired delivery verifier is approved in `delivery-source-review.md`; its saved PASS18 receipt and config-dump hash were inspected without rerunning the live proof.

## First native card frame priority follow-up

Reviewed the current `aabd2bff`-based delta in `template-parts/commerce/product-card.php` and `tools/v2-runtime/test-product-card.php`, with the unchanged native `woocommerce/content-product.php` call site and priority calculation. **No actionable finding.**

The approved decorative frame now receives the same escaped `fetchpriority` value as its existing product front. That value can be high only for index zero in an unnamed native main archive query with explicit high-priority intent. Subsequent cards remain auto-priority; their existing eager/lazy decisions are unchanged. The native loop index is read before `wc_product_class()` advances it. The frame's approved source, dimensions, empty alt text, `aria-hidden`, and asynchronous decoding remain intact. No commerce behavior, dependencies, or JavaScript change is present.

The focused regression inspects the frame tag for high/auto/auto at indices 0/1/2; surrounding existing tests cover named loops, secondary queries, and nonarchive rejection of high priority. The owner reports its red/green test passed. This reviewer performed source inspection only during the protected Lighthouse window and did not rerun PHP, browser, or performance work. Whether prioritizing the frame improves measured LCP without harming product delivery remains the owning lane's comparison, not a source-review claim.

## Inter coverage and canonical Home label follow-up

Independently reviewed the Inter `unicode-range` descriptor, Home rail label fallback, its new PHP test, and registration in the canonical package verification command. **No actionable finding.** The WOFF2 has no Git diff and the descriptor changes no font family, weight, style, display policy, or URL. Using the existing Python 3.14/fontTools installation, independently expanded the CSS ranges and compared them with the union of every Unicode cmap table in the unchanged font: exact set equality at **230 codepoints**, with neither missing nor extra characters. This proves declared glyph coverage, not a browser-rendering or performance outcome.

The Home rail iterates its fixed three canonical collection slugs. Missing or partial caller label data now falls back to the same existing collection registry; explicit names remain honored and HTML-escaped. Existing scene approval guards and native collection URLs remain unchanged. Independently ran `php tools/v2-runtime/test-home-labels.php`: **passed** for omitted/partial labels and an explicit name containing an ampersand. The package's existing `verify` sequence now includes that regression and removes no prior gate. Small checks were run only after the visual owner confirmed its font AFTER window closed. No font bytes, source, browser, or runtime were modified by this reviewer.

## Native hero poster visibility follow-up

Reviewed the uncommitted `visual-recovery.js`, `visual-recovery.css`, and controller-test delta against the prior implementation, including existing play/pause/preferences/visibility gates and the Home/collection object-position rules. **No actionable source finding.** The new class reveals the video element's real poster, assigned from the same responsive image `currentSrc` already used before this patch. Successful image decode plus nonzero intrinsic width permits the class; decode rejection does not reveal it. Existing motion source assignment still waits for the same poster-ready and viewport/preferences state, and no timer, new film source, autoplay bypass, or audit-detection branch is introduced.

Video errors and terminal play failures clear both readiness classes, revealing the retained underlying image. The previous first-playing-frame fade remains available when the poster did not decode. For a valid decoded poster, the transparent-to-video opacity fade is removed because the same native poster is already painted before playback. That is a real presentation change and is appropriate to evaluate visually; it is not a claim that the film decoded earlier or that all latency disappeared.

The two added controller tests cover decoded-poster visibility before playback without offscreen motion fetches and class removal on image/video failure. Existing tests retain preference, visibility, pause and pending-play coverage. The owner reports eight tests passed; no independent rerun was performed during root's protected single-case Lighthouse window. Independent visual review reported four accepted transition samples with unchanged film hashes; its slower raw attachment timings must remain visible in the final comparison rather than being relabeled as a speed improvement. This review does not certify performance from source or substitute an invisible placeholder for the actual poster.

## Resolved native archive frame preload follow-up

Read the new helper and route branch in `inc/performance.php`, `test-archive-preload.php`, and the actual card's presentation/frame lookup and native loop call site. **No actionable correctness finding in this delta.** The helper reads the already-resolved main Woo product query and does not invoke `the_post()`, alter its cursor/posts, initialize a Woo loop, or run a separate product-selection query. Hydration can read metadata, as the function documents. It follows existing post order and actual product visibility and returns only the first visible product's frame using the same presentation-to-collection mapping as the rendered card. An unknown first frame cannot borrow a later card's image.

Query identity/type/state, nonpublic contexts, empty/consumed/named/shortcode loops, visibility/shop-loop extensions, and category-only/mixed displays omit the hint. Missing source files remain subject to the existing SOT preload helper's readable-file guard, and native preload deduplication is retained. The tests cover exact frame selection, unchanged query/global-loop state, visibility/order behavior, deduplication, fifteen exclusions, and all-invisible results. Owner reports a passing run; no independent execution was performed in the protected Shop Lighthouse window. Root was reminded to include the new standalone PHP regression in final canonical verification/package policy.

Also inspected the Home rail PHP formatting-only follow-up: expanded statements and argument formatting preserve the previously reviewed scene guard, names, escaping, URLs, and iteration order. No semantic change or finding.

## Responsive frame delivery follow-up — source fix verified

**RESOLVED — reject symlinks in ancestor asset directories.** Independently reread the repair: each manifest/output/original path now walks every component up to, but excluding, the configured theme root, rejecting a link at any component and terminating safely if the root is not reached. This catches linked `assets/derived` and source ancestors while permitting the fixture's intentional theme-root symlink. The new `ancestor` regression relocates the derived directory and links it back, then requires empty delivery. Source and rendition must additionally decode as `IMAGETYPE_WEBP`, closing the format-integrity recommendation. The prior risk of advertising a URL that hardened Nginx refuses is addressed in source.

Other reviewed wiring is consistent: native archive cards and their first-frame preload call the same helper and consume identical source sets/sizes; missing/unknown/hash/path/dimension mismatch returns ordinary source delivery. The original 640w `src`, dimensions, empty alt/aria-hidden and priority policy are retained. Note that responsive images work without JavaScript: no-JavaScript browsers may select 384w, so describe 640w as the unsupported-responsive-image/original fallback, not a guaranteed no-JavaScript-only choice. Browser quality and generated output approval remain pending; no source or browser modifications made by this reviewer.

Focused PHP tests were read in their ten-mode form: valid, missing, source drift, derived drift, schema, path, dimensions, malformed record, ancestor link, and direct file link. Generated `assets/derived/card-frames/manifest.json` was still absent at this recheck, so the test suite was not independently executed and no generated-image approval is implied. The explicit new WebP guard is source-verified; a valid-but-wrong-format payload is not separately exercised by the current ten-mode suite.

## Native same-format font fallback follow-up — 2cb62450e

Read commit `2cb62450e4c7067f500f680bbae639de86c6963f`, the final Archivo font-face entries in `theme.json`, corresponding design-token CSS, and installed WordPress 7.1 `WP_Font_Face` validation/output methods. **No actionable finding in this configuration fix.** Core's `order_src()` indexes each URL by its extension (`$src[$format] = $url`), so multiple WOFF2 URLs in one JSON face collapse to the last URL. The corrected JSON preserves two separate faces: original Archivo first, derived normal-width Archivo second, with identical family/style/weight/display descriptors. Core validates and emits these face records in iteration order, avoiding same-format URL loss and retaining the original face as a fallback candidate. The existing direct CSS declaration continues to list derived then original URLs in its normal native CSS source list; it is not processed by Core's format map.

The commit changes only this JSON face structure and does not modify font bytes, family names, weight coverage, or script dependencies. Root reports fourteen real-browser cases passed, including derived-font failure recovery, no-JavaScript, and responsive-frame selection. This reviewer did not rerun those cases or claim new timing results; the requested five-case Lighthouse window was protected from test/build/browser work. Source approval is bounded to the configuration/order fix and does not replace the separate generated-asset and browser evidence.

## Guarded archive style bundle follow-up

Independently read `inc/archive-style-bundle.php`, its PHP contract test, `build-archive-style-bundle.cjs`, and its three Node tests. **No actionable critical/high or medium source finding.** This is materially more complex than an image hint, so source approval alone is insufficient reason to retain it: adoption still requires actual native head output/cascade verification, fallback behavior, and a measured benefit that outweighs per-request source/manifest hashing.

The runtime requires the exact seven owned handles and dependencies, a contiguous block in both queue and read-only dependency traversal, untouched source/media/extra data, no already-processed styles, and no source/tag/order filters. Unknown configuration omits consolidation. It keeps handles and dependency edges, substitutes one same-directory bundle source, and removes only the known Core inline path metadata before the native inline pass. Bundle validation binds every ordered source byte and the complete concatenation to the manifest and actual bundle. The final print guard compares snapshots and order again; rollback restores only fields still equal to the bundler's changes and preserves later plugin additions. Inspected native `WP_Styles` handling of source-less dependency handles and attached styles for compatibility. Synthetic tests do not execute the full Core inline/print sequence, so actual page evidence must confirm one bundle, no accidental duplicated inline content, and preserved cascade.

The standalone CommonJS generator rejects ancestor/source/output symlinks and traversal, reads a fixed ordered source list, rejects cross-stylesheet charset/import/namespace directives, preserves original bytes plus explicit newline separators, and writes only bundle/manifest through temporary files. Check mode performs no writes. Relative CSS URLs retain their meaning because all inputs and output share the CSS directory. A partially failed two-output update fails runtime hash validation and falls back; it does not overwrite the original stylesheets. The focused tests cover ordered bytes, deterministic/check behavior, stale output, unsafe path and unsupported-directive rejection. No async callbacks or request-handler blocking concerns apply to this synchronous build CLI.

Verification boundary: owner reports the focused PHP contract and three Node tests passed. This reviewer read those tests but did not start broad jobs/browser work. The separately invoked TypeScript reviewer stopped at the unrelated root type-check environment (missing type packages and an installed TypeScript/baseUrl incompatibility); no root type-check success or resulting source defect is claimed here. These new files are standalone CommonJS, with no scoped TypeScript project or installed scoped ESLint executable. Full theme verification, generated output/policy registration, native browser fallback/cascade evidence, and measured performance remain release gates; prior-checkpoint PR CI does not certify this new delta.

### Exact native style-source callback exception

Independently reviewed the narrow activation repair. Installed Core registers `wp_style_loader_src` at priority 10 with two arguments. Its installed implementation changes installation-relative/admin-color URLs and otherwise returns the input URL. The new helper allows only that exact callback identity, priority, accepted-argument count and solitary callback set; any extra source callback or tag/order filter still causes fallback. The outer guard now also excludes `wp_installing()`, and none of the seven owned handles is `colors`. Both initial activation and final print checks use the helper. No actionable finding.

Read the added regression covering the exact Core callback and rejection of an additional callback. Owner reports actual Shop output now contains one bundle plus the unchanged three Woo styles; that observation is attributed to root, not an independent browser run. No tests, build or browser work was started during the exclusive Lighthouse window.

## Archive theme projection replacing rejected concatenation

Reviewed the replacement generator and five Node tests, PHP projection/rollback and contract tests, generated classification structure, and the limited inline-path offer in `inc/performance.php`. The earlier seven-file concatenation review above is historical: root rejected that experiment after its measured regression. The current implementation replaces only the native `skyyrose2-theme` source with a generated projection and preserves the other six theme style handles, dependency edges and ordering.

No current shared-selector omission was found in the inspected archive, product-card, Quick View, Search dialog and associated JavaScript markup. The classifier removes only rules for which every comma-separated selector starts with an explicit off-archive owner class (or the specific native PDP `div.product` ancestor). Family boundaries distinguish `sr2-search__…` from the shared `sr2-search-dialog…`; mixed shared/owned rules, broad ancestors, negative selector mentions, keyframes and other retained rules survive. This is a reviewed source contract, not a DOM-coverage proof; interactive archive states still need the independent browser gate. Relative URLs retain their CSS directory, and the full original source/minified stylesheet remain intact and hash-bound in the receipt.

**RESOLVED — medium late-mutation rollback finding.** Initial ownership detection depended on `src === false` and the projection being the first attached CSS chunk. A later extension replacing the URL or prepending inline CSS could therefore leave stale projected CSS after rollback. The frozen repair now identifies the exact normalized CSS chunk using strict `array_search` plus the owned `inlined_src` marker, independently of its position and current URL. It removes only that chunk and its owned metadata, preserves extension source/inline changes, and restores original path metadata only when the original source URL is restored. Read the three new regressions for source replacement, prepended CSS, and both together; each asserts preserved extension output and removal of the owned chunk/marker. No outstanding finding in the repair.

The normalized CSS is prepared outside the final print callback. Known Core path/inlining transformations remain accepted; unknown changes trigger fallback. The archive projection's per-file inline ceiling is 32,768 bytes, while Core's aggregate 40,000-byte budget remains unchanged; the rest retain their 16 KiB per-file ceiling. No custom print tags, audit detection, asynchronous CSS swap, or frontend visibility change is introduced.

Verification boundary: the owner reports five Node tests and the focused PHP harness/lint passed. This reviewer inspected source and regression assertions without rerunning tests during root's exclusive Lighthouse window. The unrelated monorepo TypeScript check limitation remains as documented above; no root type-check PASS is claimed. A single exploratory CLS difference (root reports 0.000544 versus 0.000215) does not establish a visual or stability improvement: no dimensions or fonts were intentionally changed, but altered delivery timing can change rendering milestones. Attribute that difference only after comparable repeated measurements and the independent visual/state comparison.

## Five-style archive companion follow-up

Independently reviewed the new companion portion of the CommonJS generator/tests and PHP runtime/expanded contract tests, plus its generated manifest and installed Core dependency traversal. **No actionable source finding.** The exact five source files are concatenated in the existing order with one LF after each, without reminification or selector rewriting. The parser rejects sheet-level import/charset/namespace directives before writing any output; same-directory output preserves relative URL resolution. The generated receipt identifies 57,219 bytes across the five unchanged inputs plus separators. Source approval does not independently certify the generated hash or performance measurement.

Activation requires the already-active projection and its known local inline path, exact sources/dependencies/media/extra data for all five owned handles, and a contiguous sequence in both the queue and a native `all_deps()` traversal on a cloned style registry. Unknown filters or metadata omit consolidation. The first handle owns the external companion; the other handles keep their dependency registrations with source-less output. Their inline path metadata is removed before Core's pass, leaving the existing token/projection offer and aggregate budget intact. This explains the intended native head shape without custom HTML emission.

The later print guard accepts known Core projection inlining, rechecks native order/registry snapshots, and falls back if the projection rolls back or an extension changes the companion state. It restores only still-owned URLs/versions and preserves replacement URLs and attached extension CSS. Original path metadata is restored only for original URLs. Read the regression scenarios for missing/drifted files, queue/dependency interleaving, modified source/media/inline/path, late replacement/inline additions, and projection rollback. The Node additions assert exact concatenation/order/URLs and rejection of sheet-scoped directives before writes.

Owner reports seven Node tests and the expanded PHP harness passed; root reports actual WordPress output with token/projection inline totaling 35,318 bytes, one external companion and three unchanged native Woo styles. Those execution observations remain attributed to their owners. This reviewer performed file-only inspection during the protected Lighthouse window, with no test/build/browser work. Existing TypeScript-environment limitations still apply. Retention requires the measured performance result and independent visual/interaction checks; source approval is not evidence that consolidation improves delivery.

## Native first-card loading intent follow-up

Read the two-line policy change in `woocommerce/content-product.php`, its new four-case PHP harness, unchanged component eligibility checks, and canonical verification wiring. **No functional source finding.** The native loop index is captured before `wc_product_class()` advances it. Only index zero in an unnamed archive requests high priority; the second and later cards now request lazy loading. The component continues independently checking the main archive context and converts priority intent into native image attributes. Named loops remain lazy, with no changes to source imagery, dimensions, prices, forms or component API.

The harness captures consumer arguments at indices zero/one/two and in a named secondary loop, exercising the real consumer template and simulated Woo increment. Owner reports all four passed. This reviewer did not run tests or browser work during the protected verification window. Native lazy loading remains browser-controlled and may fetch images near the viewport before they become visible; parent was advised to avoid the comment's stronger claim that it never warms offscreen images. Source approval does not establish a timing benefit.

For the proposed artifact-only native jQuery experiment, no runtime change was available for approval. Core and Migrate must be treated as a pair where the installed dependency graph does not order Migrate by itself. Deferred dependent labels alone do not prove safety: actual inline-before/after scripts, enqueue order, emitter/concatenator state and final emitted tags need inspection. An isolated measurement can justify further investigation without authorizing a shipping strategy or overriding the earlier commerce race evidence.

## Final archive and responsive delivery review

The final companion replacement uses native Core inline delivery with a guarded100,000-byte head/archive allowance. A native common-style compatibility case is permitted only after proving Core would already inline it under the original40,000-byte allocation; its bytes count toward the ceiling. Independent source review found a mismatch between rawfilesize and filteredwp_filesize. The implementation now rejects pre_wp_filesize/wp_filesize filters initially and at late validation; all four new regressions passed and the reviewer verified closure. Unknown plugin/media/path/budget/filter/order changes still fall back. No outstanding source findings remain in the reviewed final PHP delta.

The optional360w frame supplement retains validated384w/640w choices and fails back to them on missing/invalid narrow output. Five new negative PHP modes and a ninth Python generator test passed. Independent PHP and Python source reviews approved the final fixed-path/hash/dimension/alpha/metadata/nonwriting guards. All eight360w dark/light contacts were independently accepted at358CSS pixels; higher-DPR/large choices remain unchanged. No jQuery strategy or concatenation change was accepted or implemented.

Canonical build and verify passed in build-archive-final.log and verify-final-archive-native.log (93Node tests,9frame tests,6font tests and the full existing PHP/media/native/source/translation checks). The final filesize-filter change then passed its focused PHP regression/syntax and source-integrity recheck. Runtime HTTP confirms seven owned inline styles, three preserved native Woo sheets and seven successful extension/fallback states.
