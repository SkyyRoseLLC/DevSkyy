# Staging browser parity — bounded PASS

The installed reviewed ZIP delivers the accepted cinematic and commerce features on staging after ordinary managed-cache revalidation. **18 final route/viewport observations and 10 fallback cases pass.** No installation-created regression was established. All browser contexts are closed.

Archive SHA-256: `e47588205c55e6303588a207d7ee766ad175b82f606d167198f4ec2c25b1d80d`. The runner used Chromium at 1440×900 and 390×900, with touch emulation at 390 and no performance throttling.

| Surface | Evidence and disposition |
|---|---|
| Five animated heroes | Home, Signature, Black Rose, Love Hurts and Kids Capsule/The Heir: 10 actual presented-frame observations across both widths. Selected sources belong to the reviewed package. All 10 compositions reviewed visually. Shop remains a native archive. |
| Nine approved scenes | 18 frame/progression observations across both widths, exact expected mobile/desktop source URLs, `contain` fit, and working native pause controls. All 18 screenshots viewed; intended scenes remain visible. |
| Product cards | Native Shop and three collection grids preserve package-bound `currentSrc` paths and responsive candidates. The Shop snapshot contains 12 cards, with 320w mobile and 480w desktop front selections. Representative coverage, not a full 33-SKU browser crawl. |
| Quick View | Both widths fetch the actual WooCommerce POST form with the correct product action. Size S resolves variation 10500 and enables the purchase button. No submission. Escape close and rapid reopen to another product retain the new title and image. |
| Search and Bag | Desktop Search and mobile Menu → Search return six preview results for “Rose.” Empty native Bag opens and closes. No search-form or purchase submission. |
| PDP and Cart | Representative SG-005 PDP native form and empty Cart shell render correctly on both widths. |
| Checkout | Both anonymous empty-cart requests redirect to Cart. This expected boundary does **not** certify checkout, payment or address flows; those remain ENVIRONMENT_BOUND. |
| Ask Skyy | Final Home waits untouched for 18 seconds on desktop and 12 seconds on mobile: static poster, zero optional 3D requests. Hover/tap then produces ready, walking-in and visible events. The preserved character, greeting, controls and composer appear in the chat dialog. No chat submission. |
| Reduced motion and no JavaScript | Five hero routes at 390 in each mode: approved posters load, video remains paused/invisible, native primary hash links work, and all 10 visible hero captures retain composition and readable CTAs. |

There were **zero page exceptions, failed HTTP resources or horizontal-overflow observations** in the final normal set. Browser source selections are bound to the release manifest; root's separate exact 527-file deployed-filesystem check supplies file identity. These checks do not independently hash every CDN range response.

## Final evidence selection

All paths below are relative to `.artifacts/v2-staging-release-20260906/`:

- `browser/revalidated/results.json`: final normal routes except Home.
- `browser/home-final/results.json`: final Home, including untouched intent-gate observations.
- `browser/fallbacks/results.json`: reduced-motion rows.
- `browser/fallbacks-visible/results.json`: corrected, visible no-JavaScript hero captures.
- PNGs are adjacent to their result files. `browser-parity.json` contains selected frame records, boundaries and evidence SHA-256 values.

The no-JavaScript fallback exposes the native directory in document flow before the hero. Initial screenshots therefore showed the directory. Five corrected screenshots scroll to the hero before capture; the original captures remain. Home uses its existing no-script concierge image and Client Services link.

## Preserved cache incident and harness corrections

Initial responses served historical HTML despite the verified installed files: accepted hero/scene-track markup was missing, and Home loaded 3D automatically. At 21:16:12 UTC, Home still reported `last-modified: 21:10:32` with STALE/Batcache-Hit headers. Ordinary same-URL revisits recovered current markup; final Home then passed. Initial results remain incident evidence and are superseded by the final selection above. This reviewer used no cache-busting query, bypass header or purge. Root's separate natural-revalidation record documents the wider cache timeline.

Initial harness labels were not treated as final certification. Search initially accepted idle text before its debounce completed; mobile Search initially selected a hidden desktop trigger; and Skyy readiness alone did not prove intent gating. Final runs require completed preview results, the native visible Menu → Search path, zero pre-intent optional requests, and the actual chat composer. Corrections affected artifact test helpers only.

## Boundaries

No remote content, configuration, product, model or database writes occurred. No add-to-cart, order, payment or chat submission occurred. Passive page requests were allowed normally. Cookie, authorization and nonce values, and raw personalized form HTML, were not persisted. Form evidence records method/action, field names/types, disabled state and the selected product variation; cache headers are allowlisted.

The Quick View close/reopen check used actual network responses. It does not guarantee that an abort happened while a response was pending, and no delayed response was manufactured. Screenshots support composition review and video callbacks support sampled progression; they do not certify entire loop seams or new garment fidelity. This is Chromium with viewport emulation, not physical-device or all-engine certification. Existing performance, field-INP, model-payload and content-approval blockers remain independent.
