# Exact staging release package — review required

A runtime ZIP has been prepared from the accepted working tree. Canonical build and verify pass; runtime bytes match the accepted source exactly. A supplemental historical test had a stale line-number assertion; its original failure is retained and a disclosed export-only correction now passes7/7. Independent runtime dependency review remains pending. This package is not a production-readiness claim or a deployment receipt.

- ZIP: `.artifacts/v2-staging-release-20260906/package/skyyrose-flagship-2.zip`
- SHA-256: `e47588205c55e6303588a207d7ee766ad175b82f606d167198f4ec2c25b1d80d`
- Size: 170,023,102 bytes; 527 runtime entries; unchanged theme version2.4.4.
- Accepted source digest: `4a23629d6b17c1d0393000cfbbe982eedf47712837dd772a66c993ad7e46bbe7` (86 recorded files verified).
- Historical HEAD: `aabd2bffdc1e322862acd05a5640c14cf00f3acf`; source is dirty. It is not a clean HEAD build.
- Exact isolated export Git tree: `5dd1902d8075e9c98bafaf5f80e3a88f2792a3de`, including the disclosed packaging metadata overlay. No commit was created.

## Identity and package boundary

`source-inventory.json` records all608 theme files plus97 required tooling/catalog/source-evidence files, each with SHA-256, and original dirty/untracked status. Theme traversal includes relevant untracked files; dependencies, old dist output and Python caches are excluded. All705 original input files still match after the isolated work. This is the complete selected build export, not an assertion that the entire monorepo is clean or inventoried.

The historical package boundary covered588 theme files and rejected20 additions. Parent authorized an artifact-local overlay only. `packaging-overlay.json` records every classification and the before/after hashes of package-boundary.json and its pinned build-inputs.json reference.19 added runtime files ship. `data/font-fallback.json` is excluded as build-only subset reproduction provenance: a theme-wide text search found no consumer outside the recipe itself. The recipe contains relative paths; exclusion is not a claim of a workstation-path leak. Its derived font and license continue to ship. The original recipe remains in the export/inventory.

No shared source, generated output, packaging script, catalog, approval or version was changed. The later export-only test correction is disclosed below. The existing package.py ran unchanged. The export contains a local Git object database solely to compute the exact snapshot tree. A node_modules symlink supports isolated verification; neither it nor Git metadata is packaged. Original media and accepted runtime bytes are preserved.

## Verification

1. Pinned Node22.23.2/npm10.9.8 and Python3.12.12/Pillow12.3.0 canonical build passed. Every inventoried source/generated byte matched after rebuilding; log `build.log`.
2. Canonical npm verify passed, including95/95 JavaScript tests, PHP syntax/native Woo fixture boundaries, assets/tokens/catalog/media/scene approval/i18n generated checks; log `verify.log`. Fixture was the existing19db v2-phase3 WordPress fixture, selected through V2_WP_FIXTURE. No browser, server or remote mutation was run.
3. Packaging-overlay integrity check passed without weakening the validator.
4. Two consecutive unchanged-packager invocations produced the identical ZIP hash above. This proves deterministic serialization of this frozen built export; it is not a second independently provisioned toolchain build.
5. Archive CRC, explicit sorted allowlist, source-to-entry SHA-256, regular-file modes, safe paths, and absence of internaltooling entries passed. Additional text scan found no absolute workstation paths or private-key/API-token markers. Bounded scanning does not prove absence of every possible secret. Per-file runtime hashes and toolchain are in `release-manifest.json`; excluded81 source files and reasons are in `package-validation.json`.
6. Additional historical Python source-certification suite:6/7 passed. `test_i18n.py:19` hard-codes `template-parts/commerce/product-hero.php:55`; current accepted source and correctly regenerated POT both report line57. The actual plural message is retained. Log `source-certification-tests.log`. At this initial run no test was edited or waived. Canonical verify passes its generated POT parity check. The subsequent authorized export-only test correction and retained original failure are recorded below.

The initial isolated runs exposed missing external scene approval records and three legacy source images; these exact required inputs were copied into the verification export, never into runtime. They are included in the705-file inventory. Those setup failures were resolved without changing validators or source.

## Reproduction and boundaries

Use the recorded pinned Node/Python paths in build/verify logs and V2_WP_FIXTURE, run npm build and npm verify in export/wordpress-theme/skyyrose-flagship-2, then run `finalize-package.py` with the same toolchain PATH. The script verifies theme hashes, derives the actual Git tree, invokes package.py twice and checks archive contents. Packaging metadata overlay and export remain inspectable. There is one runtime ZIP at package root; transient export/dist was removed.

Known mobile performance and product-content/media approval blockers remain. This artifact supports bounded staging parity review. No deployment, cache purge, transaction, production approval or source promotion occurred here.

## Supplemental test reconciliation and review hold

Parent authorized an export-only test correction after preserving the original7-case failure. The test now locates the exact native plural call, requires precisely one occurrence, and compares the extractor reference with its actual line. Plural identity and rendered plural assertions remain unchanged. `test-overlay.json` binds the accepted and corrected test hashes; `source-certification-tests-reconciled.log` records7/7PASS. The export Git tree above and sidecar were refreshed; the ZIP hash is unchanged. Original accepted/shared test source remains untouched. The earlier failed historical test is not erased.

Independent reviewer reports exact PHP source-versus-ZIP resolution passed: three scenes per collection, all nine complete arrays identical. Excluded founder placeholders and four old composition masters are overwritten by final motion selection; the final-nine gate prevents fallback to those excluded assets. Hero master films/QA serve provenance, while runtime reads approved delivery variants and source stills. The reviewer also found no functional weakening in the test overlay. Final independent ZIP byte-parity/deployment-plan disposition remains pending; do not deploy based on this package report alone.
