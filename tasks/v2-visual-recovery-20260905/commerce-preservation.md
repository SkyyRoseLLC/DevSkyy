# Visual recovery: commerce preservation and motion boundaries

Status: **INDEPENDENT READ-ONLY REVIEW — SELECTIVE IMPLEMENTATION AUTHORIZED TO ROOT.** The founder rejected Phase 3B's visual direction. Earlier engineering approvals and reduced payloads do not constitute creative approval. This report identifies engineering boundaries to carry into a visually ambitious recovery; it does not recommend keeping Phase 3B's simplified composition.

The inventory request was attachment `622959fa-12ce-49fd-b8aa-bf7a13aa1fb9/pasted-text.txt`; subsequent attachment `75f5cfa8-c01e-4a3d-8990-77a2496af8be/pasted-text.txt` authorizes selective integration and makes Ask Skyy mandatory. This reviewer inspected source and existing evidence only. No recovered PHP/JavaScript was executed; no browser, build, database, plugin, payment, deployment or runtime edit was performed.

## Three-way authority

| State | Exact source | Meaning |
|---|---|---|
| A | `bb0e6eecf0af54b3f0c14e5eadb7b138530ce09b` | Exact recovered staging theme and visual evidence. Extraction inspected at `.artifacts/v2-visual-recovery-20260905/recovered/skyyrose-flagship-2/`. |
| B | `fec4e9339cad7077bcc812876668c128eb93b6e4` | Current repaired runtime. Preserve its commerce/security/accessibility behavior while recovering visual presentation. |
| C0 | `ddb3dce6db82d69010f8d15fee4fbb6b99661066` | Certified canonical build/source baseline before Phase 2 runtime repairs. |
| C1 | `5de8e2f3eb40827a052996f72bfb290a95bd600a` | Certified Phase 2 repair source; preferred comparator for behavior that must survive the visual merge. |

Git merge-base checks confirm C0 is an ancestor of C1 and C1 is an ancestor of B. `git show` confirms all four full commit identities. Representative extracted A files `functions.php`, `assets/js/theme.js`, and `template-parts/commerce/product-hero.php` exactly match A's Git blob IDs. This is a targeted source check, not a fresh all-447-file archive certification by this reviewer.

The prior reconciliation report records archive SHA-256 `3850af0ae0caa20a49e8ec4f029ef1543be4ac5ef65202b2a1a0336293f09f4a` and 447 matched regular files. Critically, that archive contains **the theme only**, not WordPress, plugins, database, media-library uploads, orders or historical sessions. A rendered against today's fixture is a reconstruction with explicit environment differences, not proof of exact historical staging output. Full extraction/hash attestation belongs to the parent inventory.

All theme paths below are relative to `wordpress-theme/skyyrose-flagship-2/`; A paths mean the same relative path in the recovered extraction. Existing Phase 2 receipts describe historical tests, not fresh recovery validation.

## Behaviors the visual merge must preserve

| Boundary | Evidence in A and repaired C/B | Preservation rule for D |
|---|---|---|
| Native product identity and sizes | Phase 2 `product-model-reconciliation.md`, `variation-runtime-repair.md`, `REPORT.md`: 31 size-required variable parents, two legitimately simple products, 185 native variations. This repair includes database state and migration journals, not just templates. | Keep current parent IDs, exact SKUs, variation IDs/attributes, prices, URLs, categories and stock semantics. Do not reimport A's historical presentation data as Woo product configuration. Do not invent per-size quantities. The local fixture's variation IDs are not the historic staging IDs. |
| Purchase readiness and duplicate submission | A `assets/js/theme.js:687–741` marks readiness on `found_variation` and does not cancel already-busy submits. B's native commerce adapter uses `show_variation`, checks the real hidden variation ID, refuses absent/disabled selections, cancels busy duplicate submission, and resets presentation on `pageshow` and native cart events. | Retain B's complete adapter and native form controls. Recovered button styling may wrap them; visual motion must not calculate product availability, forge variation IDs, replace validation, or intercept a native valid purchase. |
| jQuery dependency order and CSP | A `inc/performance.php` adds Home-only `jquery-core` defer; C removes it after observed optimizer/dependency ordering errors. C/B `skyyrose2_performance_csp_variation_templates()` prepopulates only two standard Woo template renderers and rejects unsupported template syntax, avoiding Underscore dynamic compilation. | Keep the repaired enqueue ordering and CSP-safe native template cache. Never restore A's entire performance file or add JavaScript `unsafe-eval` to make an old effect run. The current security headers already permit inline scripts and WebAssembly evaluation: they are not a general sandbox or a newly hardened nonce policy. |
| Cart controls and fragments | A `woocommerce/cart/cart.php` wraps `woocommerce_quantity_input()` in post-content KSES, stripping form inputs. C/B render Woo's already-escaped native control directly. B retains jQuery `added_to_cart`/`wc_fragments_refreshed` handling and native collection action assets. | Preserve quantity inputs, nonce/removal/action hooks, real variation line items and fragments. Restore framing around the native cart; do not copy the old input sanitizer or substitute a local visual cart total. C→B diff of this cart override is empty. |
| Checkout, order hooks and payment truth | A `woocommerce/checkout/thankyou.php` says “Nothing was charged” and manually includes order details before Woo's registered callback. C/B use seven conservative status messages and preserve native before/gateway/generic thank-you hooks once. C→B diff is empty. | Keep native checkout and status-aware acknowledgement. Do not restore unsupported no-charge assertions, duplicate order details or submission handlers. Neither prior verification nor this recovery certifies an actual payment, refund, authenticated order page or gateway redirect. |
| Account repair | Phase 2 `account-apply.json`, `account-idempotency.json`, `REPORT.md` identify a guarded page-content migration to `[woocommerce_my_account]` using Woo's page assignment. | Preserve native page content and assignment. Restoring PHP alone cannot recreate or validate this repair. Do not run the old demo/page importer or replace account content to match an old screenshot. Authenticated account flows remain outside prior test coverage. |
| Pre-order truth | A `woocommerce/single-product.php` promises made-for-order/fulfillment timing; old Home/service/pre-order copy also described unsupported reservation assurances. C removes those claims. B's PDP explicitly states standard checkout and no reservation/deferred-payment effect from the label. | Recover visual edition/launch treatments while retaining truthful labels and native checkout. Registry flags are presentation, not a transactional pre-order engine or allocation. Phase 2 recorded a live-configured Stripe gateway; never assume the recovered environment is a payment sandbox. |
| PDP media authority and native hooks | A `template-parts/commerce/product-hero.php:30–84` requires its verified-card media list, skips the entire before-summary action when media is absent, and restores temporary getter filters without `finally`. C/B `skyyrose2_product_commerce_media()` separates permitted editorial attachments, assigned commerce fallback and explicit rejection; B retains hook execution and exception cleanup. | Restore dramatic PDP composition around the repaired media resolver and native action sequence. With missing imagery, suppress only the native gallery callback where needed; keep extension callbacks. Never infer PDP permission from accepted card media, filenames or old visual placement. |
| All native gallery representations | B adds `inc/pdp-media-delivery.php` and `woocommerce/single-product/product-image.php`, stable parent permission captured before Woo overrides, and priority-40 variation HTML repair. Woo 11.1 emits visible gallery, template reset snapshot, inline default cache and variation gallery HTML; image JSON alone does not govern them. | Preserve the shared rendering boundary, candidate-ID intersection/order, rejected/missing empty state, known `get_variation` AJAX scoping and `finally` cleanup. Preserve the image-only resolver's guarded video-metadata boundary. Do not recover the old hero by discarding these wrappers. |
| Responsive delivery without authority changes | B `inc/approved-card-fronts.php`, `inc/pdp-media-delivery.php`, derived card manifest and native gallery filters check accepted byte identity. Display derivatives preserve native IDs, alt, full/lightbox and thumbnail references; hover zoom is disabled on PDP while explicit PhotoSwipe remains. | Recover the old card's silhouette, framing, typography and scene relationships around canonical card facts and explicit `sizes`/priority. Do not discard same-source derivatives or preload a rejected original. Scene/editorial media may remain in its legitimate existing context without becoming PDP imagery. |
| SEO media and preview policy | B active adapter `inc/seo-indexing.php`, legacy fallback and performance preloads use the permitted primary or omit it. Nonproduction previews intentionally omit social meta. | Keep approved/rejected media decisions consistent in visible markup, structured data, OG/Twitter and preloads. Preserve preview noindex/nonproduction policy. A successful screenshot does not validate SEO metadata. |
| Search grouping | C/B `search.php` and `functions.php` use a bounded native main query, mutually exclusive groups, at most one exact visible/published parent-SKU supplement and no builder-page excerpt leakage. A retains the earlier overlapping result treatment. | Restore search visual treatment without restoring duplicate groups, raw builder excerpts or unbounded duplicate queries. Keep GET URLs, native pagination and escaped query text. |
| Shop query integrity | B `inc/shop-archive.php` restricts stock filtering to the native main product query, preserves other meta/tax filters, retains scalar hierarchical/unknown category routing and rejects malformed container inputs. Native ordering/no-JS GET state remain. | Scenes or collections may frame results, but must not overwrite query context, erase valid taxonomy paths, alter product order/count silently, or rewrite native pagination/filter URLs. |
| Overlay focus and scroll ownership | A `assets/js/theme.js:41–63` toggles menu classes without background isolation; separate Quick View/size/search lifecycles live later in the same file. B has one native-dialog/navigation coordinator, inert restoration, focus trapping/return, one active surface and scroll restoration. Phase 3B also preserves modified-click anchor navigation. | Recover navigation/overlay artwork and transitions around B's coordinator. Do not transplant A's `setMenu`, dialog functions or second scroll-lock system. Preserve Escape, native top layer, trigger focus, modified-click/no-JS destinations, safe-area limits and visible focus. |
| Mascot/character | C/B `assets/js/mascot.js:568,650,742,801` prevent mobile proactive entrance, respect open overlays and avoid delayed focus theft. Phase 2 CSS places mobile controls in flow before the footer. The mascot artwork/3D source remains available. | Restore meaningful character presence without undoing CTA clearance, mobile layout or focus rules. This is not authorization to erase the mascot; it is a requirement to integrate its restored expression with the repaired interaction system. |
| Source/build reproducibility | C0 canonical contracts, C token derivation, B strict PostCSS preparse before output writes, pinned npm shrinkwrap and classified generated/runtime files prevent source/minified drift. Prior A capture includes deployed/generated bytes with historical normalization differences. | Extract valuable source deliberately, rebuild generated artifacts only in the later authorized implementation, update classifications transparently and preserve immutable evidence. Never overwrite B's complete tree or reclassify discarded visual assets as obsolete merely because they are not currently enqueued. |

`inc/security.php`, `woocommerce/cart/cart.php` and `woocommerce/checkout/thankyou.php` have no C1→B content delta in the inspected Git comparison. The security file's protections are largely inherited rather than a new Phase 3 redesign. Preserve them without overstating certification.

## Recovered motion systems: source-based classification

These are engineering dispositions for an eventual recovery, not authorization to enable motion now. None grants asset rights, product authenticity or founder visual approval. “Refactor” means retain the intended effect and fix its lifecycle/integration; it does not mean replace it with a flat section.

| Recovered system / evidence | Classification | Keep visually; engineering boundary |
|---|---|---|
| Native rail scrolling and previous/next controls, A `theme.js:356–394`; base rail remains in B | **SAFE TO RESTORE** | Preserve native scroll transport and reduced-motion auto behavior. Reuse the surviving base helper instead of importing the monolithic A script. This disposition excludes the pinned transform mode below. |
| Viewport-selected collection videos, A `collection-scene-motion.js:7–134` | **RESTORE WITH REFACTOR** | Existing source already arbitrates one active video, chooses mobile/slow-connection source, honors reduced motion/Save-Data, handles autoplay denial and pauses hidden/offscreen media. Retain that richness. Add explicit observer/listener ownership for teardown/reinitialization and integrate with route ownership; current observer is not retained for disconnect. Verify source-media contract separately. |
| Collection hero effects/video, A `theme.js:109–214` | **RESTORE WITH REFACTOR** | Preserve layered hero/poster relationships, motion toggle and viewport gating. Extract this section from commerce/overlay code, pair observer disconnect with restoration after page cache return, and preserve preference changes. Restore only its declared world assets. |
| Desktop pinned Scroll World, A `theme.js:218–350` | **RESTORE WITH REFACTOR** | Source uses passive native page scroll, rAF throttling, geometry-derived height, ResizeObserver and desktop/fine-pointer/reduced-motion gates. Do not call it wheel hijacking: no wheel cancellation was found in this helper. Add Save-Data eligibility and visible keyboard focus handling for offscreen transformed product links; test native scroll restoration and responsive transitions. Keep depth/pacing, not the old integration assumptions. |
| Town Line film/chapter/scroll timeline, A `house-of-roses-motion.js:278–end` | **RESTORE WITH REFACTOR** | Source already uses lazy sources, poster fallback, reduced-motion/Save-Data gates, muted opt-in/sound controls, viewport pause and AbortController cleanup. Desktop scrubbing seeks video over a minimum four-viewport interval. Keep film/chapter composition; profile seeking and revisit repeated page-cache cycles (`pagehide` cleanup is once-only but `pageshow` can reinitialize). Retain previsualization/candidate labels and no implicit commerce-media promotion. |
| Header image reel, A `house-of-roses-motion.js:70–276` | **RESTORE WITH REFACTOR** | Preserve layered header pacing where founder prefers it. Source has pause-on-focus/pointer and responsive eligibility. Fit it around the current shell/overlay lifecycle and give repeated page-cache restore cycles symmetric cleanup; do not restore the old navigation implementation. |
| Kids Royal Procession, A `kids-capsule-reveal.js` | **RESTORE WITH REFACTOR** | Native scroll, three chapter states, reduced-motion/Save-Data, image fallback and AbortController are useful retained work. The controller aborts on `pagehide` without a `pageshow` rebind; reconstruct it on page-cache restore. Keep prior guardian/playroom versus throne media-contract distinctions visible. |
| Home model loop, A `theme.js:625–678` | **RESTORE WITH REFACTOR** | Preserve on-model layering and pause UI only with permitted existing editorial context. Observer is disconnected on `pagehide` without re-observation; preference and Save-Data handling must stay live and desktop motion must not duplicate inaccessible/focusable copies. Restore motion logic independently of A purchase handlers. |
| Product hover/focus World Reel, A `theme.js:416–449` | **RESTORE WITH REFACTOR** | Existing 1500ms interval stops on pointer/focus exit but has no page visibility/pagehide cleanup or Save-Data gate. Keep product staging/world transition around the canonical card, add lifecycle/pause handling, and never replace garment proof, product URL or Quick View facts with a scene. |
| Old continuous hero video fade/replay, A `theme.js:557–622` | **RESTORE WITH REFACTOR** | Existing source has desktop/preference gates and unload behavior, but replay uses a delayed play callback and separate fade rAF. Give all timers/frames a common cancellable lifecycle so hidden/pagehide state cannot revive playback. Avoid two video controllers owning one hero. |
| Pointer depth, image reveals, headline split and Bay map progression, A `theme.js:90–107,532–554,744–end` | **RESTORE WITH REFACTOR** | Preserve offsets, typography and spatial staging. Reuse B's visible initial DOM/focus controls; no structural commerce heading should wait for an observer. Add current preference/lifecycle gates and avoid text-splitting after paint that changes line geometry. A blanket removal because these effects cost code is not justified. |
| Dedicated immersive WebGL, A `immersive.js:123–255,423–end` and `template-parts/immersive/world.php` | **RESTORE WITH REFACTOR** | Existing fallback, visibility pause, DPR cap1.75 and disposal are valuable. Source disposes on pagehide without a clean pageshow reboot; scene observer is not disconnected and can retain the disposed world. Reimplement lifecycle around unchanged atmospheric/story intent. Dedicated immersive browser coverage was absent in the earlier local fixture and remains unverified. |
| Skyy 3D walking character, A `skyy-3d.js:73–115,196–212` | **RESTORE WITH REFACTOR** | Preserve canonical character and low-power renderer/hidden-loop behavior; integrate with C/B mobile/overlay/consent timing. Local GLB is about5.8MiB, so keep conditional loading and static fallback. File size is evidence of cost, not proof that the character must be removed. |
| Copying A's entire `theme.js`, `functions.php`, PDP or checkout override to recover an effect | **CONFLICTS WITH COMMERCE** | These files contain the documented repaired behaviors above as well as visual code. Extract/reimplement visual parts; retain B/C commerce/security/accessibility ownership. |
| Restoring A's mascot proactive mobile entrance and fixed obstruction with its visuals | **CONFLICTS WITH COMMERCE** | Prior Phase 2 evidence identified mobile obstruction and focus theft; restoring those behaviors can cover purchase controls or interrupt dialog focus. Recover character prominence through compatible layout and activation. |

No complete visual system is classified **TOO EXPENSIVE** solely from this source inventory. The archive contains roughly2.3MiB MP4/1.5MiB WebM Town Line alternatives and a5.8MiB mascot GLB; playback/fetch selection matters, and no matching recovery performance measurement has run. No complete visual system is discarded as **TOO FRAGILE** where a bounded lifecycle or integration repair is apparent. Specific unsafe wholesale transplants are classified above. Profiling may later establish a narrower effect/budget conflict, but it must be measured and compared to a preserved visual alternative.

## Isolated rendering of A: risks and required containment

The current assignment is inventory only. These are prerequisites for a later authorized comparison, not changes made by this reviewer.

1. **Use a disposable fixture and read-only database access.** Do not point A at staging, production or the existing repaired fixture's writable database. Merely opening a GET can cause Woo session/transient/cache writes or trigger scheduled work; read-only file mounts do not protect a database. Snapshot any disposable import first, then enforce a SELECT-only runtime credential for capture and disclose any resulting rendering limitations. Do not weaken containment to make a page look complete.
2. **Deny outbound traffic at both layers.** Browser origin interception does not block PHP/plugin HTTP, SMTP, cron or action-scheduler work. Use network containment for PHP/container and browser, plus disabled cron, blocked mail and HTTP as defense in depth. The inherited CSP explicitly permits external images, scripts, APIs and payment frames; it is not egress isolation. A `functions.php:552` calls `wp_mail` from its contact submission path.
3. **Block mutation endpoints and navigations.** Comparison capture should not submit forms or follow add-to-cart/remove-item query links, checkout/order-pay, account/password-reset, contact/admin import or mutation AJAX/REST routes. A `inc/demo-import.php:84,167,198–205,272–305` can create pages/menus and alter assignments; theme activation schedules a transient even though full import remains opt-in. Do not activate the theme or run migrations on a shared database to obtain screenshots.
4. **Keep environment differences explicit.** A's theme-only backup cannot restore historical Woo data/plugin behavior. Current native variations should remain intact in B; render reconstruction should disclose the fixture/catalog/plugin version rather than presenting current fixtures as old staging truth. Staging gateway settings were historically live-configured; no payment should be submitted even on a visual comparison.
5. **Attest the served source before each capture.** Earlier Phase 3B baseline switching showed PHP realpath caching can retain candidate templates while static assets follow another symlink. Separate PHP processes/ports per state are preferable; otherwise restart and attest expected markup plus served CSS/JS hashes. A snapshot/archive SHA alone does not prove the active page used those bytes.
6. **Preserve original A and B as evidence.** Use read-only copies/mounts and keep any environment shim outside the theme tree. Never modify A or substitute rebuilt minified output and still call it exact recovered staging. If guarded rendering differs, label it reconstructed A under containment and keep source evidence alongside the capture.

## Follow-on engineering gate after founder-directed visual implementation

Before promoting D, rerun the existing native purchase-state, cart quantity, media hierarchy/default/variation gallery, search, Shop query, pre-order/payment messages, overlay/guide focus, token and source/package tests against the restored candidate. Run keyboard/no-JS/reduced-motion/Save-Data and native-scroll checks on restored motion itself, including repeated Back/Forward restoration and viewports with purchase controls visible. Compare A/B/D imagery at identical viewports and attest data/source state. Keep authenticated account, real payment, media-rights and dedicated immersive gaps explicit until actually covered.

The founder's later directive authorizes selective integration under the file allowlist; this report does not broaden that authorization to blanket restoration, new imagery, deployment or a different creative direction.

## Selective integration review under 75f5

Reviewed the initial `founder-visual-allowlist.json` and `three-dependency-receipt.json`. The allowlist correctly identifies B as the engineering base, keeps product-card/media authority and PDP delivery, and limits the first visual comparison to Home / Black Rose / PDP plus mandatory Ask Skyy. Its Skyy GLB and static fallback hashes match recovered A. Vendored Three r170 is a supporting dependency, with separate upstream and rewritten-file hashes; library availability is not proof that the character renders, animates or answers questions. Current four Home/Black Rose edited paths are allowlisted. No PR merge readiness is implied; this is a local review, and the V2 package has no TypeScript or ESLint command.

Actionable findings sent to root for the ongoing implementation:

1. **Skyy readiness bypass:** existing `assets/js/skyy-3d.js` accepts an animation-free GLTF via the `else { modelReady = true; }` branch after its six-clip validation. Require the complete canonical action set before readiness/reveal. Test absent animations and partially missing clips as failures, plus actual loaded rig/action behavior as the positive case. A blocked CDN does not establish a missing or invalid model.
2. **Guide/API integration gap:** existing `mascot.js` references `SKYY_GUIDE_DATA`, `SKYY_MASCOT_CONFIG.guideUrl` and optional `skyyrose_mascot_chat`, but whole-theme PHP source has no corresponding guide configuration or AJAX handler. The current free-text fallback cannot be presented as verified guide/API integration. Wire the canonical local guide, retain text-node rendering, constrain any destination to permitted site URLs, and disclose optional LLM availability without inventing responses or making paid calls.
3. **Black Rose preload mismatch:** the new arrival markup uses `sizes="100vw"`, while `inc/performance.php` still preloads the shared helper's desktop 60vw slot. Make the shared Black Rose media decision feed both preload and markup; otherwise responsive selection can fetch two hero candidates. Other collection slots must remain unchanged.

Exact preservation gates for the integrated candidate:

| Boundary | Existing verification seam | Required integration evidence |
|---|---|---|
| Native purchase readiness and safe variation templates | `tools/v2-runtime/test-purchase-state.cjs`, `test-csp-variation-templates.cjs` | Actual native select → reset → select; unresolved variation cannot submit; no duplicate busy submission; jQuery present before dependent code. |
| PDP identity and all native gallery representations | `test-product-media.php`, `test-pdp-delivery.php`, `test-pdp-gallery.php`, `test-pdp-summary.php` | Accepted product delivery and rejected BR-003 remain unchanged across rendered gallery, default templates/cache, variation JSON/HTML and native AJAX. Original PhotoSwipe/full-image authority stays native. |
| Cards, Shop, search and collection route isolation | `test-product-card.php`, `test-card-renditions.py`, `test-shop-archive.php`, `test-search.php`, `test-collection-routes.php` | Real product destinations, native stock/price, canonical source bytes, responsive slot parity, unchanged non-Black-Rose routes, no query broadening. |
| Commerce truth and indexing | `test-checkout-truth.php`; theme `scripts/test-seo-indexing.php`, `scripts/test-performance.php` | No guessed payment/pre-order claims; permitted image preload/SEO only; preview noindex policy stays. No live payment required. |
| Shared overlay and guide focus | `test-overlay.cjs`, `test-shell-overlays.cjs`, `test-guide-focus.cjs` | Skyy opens/closes alongside search, bag, Quick View and fit guide without competing modals, focus theft, stale inert state or purchase-control obstruction. Keyboard, modified clicks and fallback remain usable. |
| Deterministic source/build | `test-css-build.cjs`, `test-token-system.cjs`, package `build` / `verify` | Strict CSS parse, regenerated tracked minified outputs, scoped source hashes and immutable canonical media. No whole-theme copy to pass checks. |
| Mandatory Skyy behavior: new evidence required | No existing test currently proves the complete 3D/chat integration | Real canonical model/rig/action initialization; walk-in, idle, talk, wave, joy and exit; visible controls; guide answer and actual destination; denied/failed load fallback; Save-Data/reduced-motion; mobile purchase controls; repeated hide/show and Back/Forward. No mocked readiness presented as browser proof. |

Root's in-progress recovery receipt records A Home at 390px returning HTTP 200 with `jQuery is not defined`, no HTTP errors and no blocked requests in that case. This corroborates the old runtime fragility; it does not justify modifying A or claiming the entire A capture matrix complete. Preserve A unchanged as comparison evidence and carry B's repaired dependency ordering into D.

## Mandatory-system candidate source review — 3b686012

The later founder contract `3b686012-fe90-4073-ae81-e1287755a211` requires final animated heroes, nine approved final collection scenes, Ask Skyy and final paid cards. Town Line is preserved for later Pre-Order implementation. This supersedes the earlier three-route stopping point. Another independent reviewer owns final scene membership and asset provenance; this review covers runtime commerce/security/lifecycle seams, not creative approval.

Read the local staged/unstaged/new source scope: `mascot.js`, `mascot-loader.js`, `skyy-3d.js`, `visual-recovery.js`, `skyy-mascot.php`, the card delta and root's new guide localization. No runtime was executed by this reviewer.

Resolved at source:

- Skyy now requires a skinned rig and all six populated, positive-duration clips before readiness. Empty-animation acceptance is removed.
- Root localizes deterministic guide answers and a bounded published Woo catalog query, excluding invisible and password-protected products. Names, SKUs and permalinks remain native; answers do not invent price, stock, order status or shipping promises. The UI explicitly distinguishes the site guide from live support.
- Answer text enters `textContent`, links are same-origin HTTP(S), the transcript is bounded, input is limited to 300 characters, and guide interaction performs no remote chat request or commerce write.
- The local module graph closes over `three.module.min.js`, `GLTFLoader.js`, `DRACOLoader.js` and `BufferGeometryUtils.js`; the loaders/utilities import the same local Three instance. The receipt distinguishes upstream bytes from import-specifier rewrites. Native dynamic imports have a handled timeout; model fetch rejects redirects and aborts after its timeout. No new CDN requirement is introduced by this graph.
- Card changes add the approved decorative statue and collection inscription while preserving current product/media resolver, responsive source, price, stock, native product destination and Quick View behavior.
- Shared arrival media now uses the same 100vw slot as the current full-width collection hero markup/preload. Earlier Black Rose mismatch is resolved for the current wider rollout.

Actionable issues returned to the author before final approval:

1. **Medium — lost motion control after preference changes.** Reduced-motion or Save-Data on emits fallback and hides Pause; returning to normal resumes the renderer without restoring Pause. Synchronize the stop control with the visible running renderer and test the on → off transition.
2. **Medium — delayed invitation can steal focus.** When first guide loading takes time, fulfillment unconditionally opens the dialog even if the visitor moved to another field. Preserve the initiating focus/intent and leave the successfully loaded guide ready for a subsequent invitation when that ownership changed.

Wave/joy/exit handlers initially had no actual UI emitter. That is an evidence gap for any claim that those actions are integrated; the latest contract specifically requires walk-in and engagement, so absence of every individual gesture is not independently a commerce blocker. Keep native Escape immediate rather than trapping visitors for an exit animation. Positive WebGL/browser evidence remains required; the new unit tests' GLB metadata assertions are not renderer or visual fidelity proof.

**Targeted recheck: source gate PASS, browser gate pending.** Both medium findings are resolved in the refrozen source: the renderer emits `skyy:3d-visible` when it becomes visible again and the guide restores Pause; first-load completion checks document visibility and retained initiating focus before opening. A late 3D script cannot boot after its loader timed out. Actual greeting/product-match/Close controls now emit wave/joy/exit; the Close button has a bounded 450ms exit only with active motion, while native Escape remains immediate. The author reports 11 focused tests passing; this reviewer inspected their assertions and code, and ran only a scoped read-only `git diff --check` (clean). No independent test execution or browser rendering was performed here. The required next evidence is real WebGL initialization and action playback, preference transitions, focus/overlay behavior, mobile control clearance, and matched visual/performance capture.

The reviewed `visual-recovery.js` uses native scroll controls without wheel interception, keeps CTA markup independent of animation, gates video playback on visibility/preferences/pause, handles play rejection and late resolution, and reattaches its hero observer after Back/Forward restoration. Its source introduces no commerce transport or product-state override. Scene approval membership remains the separate canonical-manifest reviewer's gate.

## Final frozen-source recheck

**No new actionable commerce/security defect found in the reviewed final delta.** Inspected final scene manifest/checker, pinned poster builder and 19 tests, runtime scene filter, BR3 responsive-poster template, localized guide, header invitation, package classifications, Three pins, and Skyy decoder/focus follow-up. The required local Draco loader is now attached to GLTFLoader, limits its worker pool to one and disposes workers after parsing or teardown. Earlier source approval did not prove compressed-model decoding; this actual-browser-discovered defect is now corrected.

The scene validator binds exactly nine scene IDs/order/collections, explicit approval evidence hashes, the 27 selected poster/video files, native SKU bindings, no added layers, and only the two bounded BR3 supporting derivatives. The poster builder invokes `cwebp` through fixed `execFileSync` arguments, pins encoder/libsharpyuv versions and parent hash, rejects unapproved dimensions/destinations, validates reproduced bytes and preserves the original. The PHP filter rejects scenes outside the final selection or with substituted product bindings/layers. BR3 responsive delivery preserves its selected parent authority. The five superseded PNG classifications change only package inclusion, preserving source. Excluding the exact vendored Three directory from the placeholder-word scan is bounded by all five dependency/license file hashes in the integrity inputs.

Validation limits:

- `check:approved-scenes` without arguments attests the manifest/assets, not executed PHP output. Its unit `resolverFixture()` is built from the manifest. Actual resolver attestation additionally requires a read-only PHP export passed to `--resolved`; root was asked to retain that receipt before claiming this coverage.
- Read existing `verify-final.log`: 64 Node tests report pass with zero failures; downstream card, Shop, native PDP summary/delivery/gallery, tokens and rendition checks also report pass. The 19 scene tests are included in that total. This reviewer did not rerun commands while serial Lighthouse work was active.
- Read existing `D-skyy-3d-verification.json` and `D-skyy-fallback.json`, both PASS with `sourceOverride: false`. These provide supplied browser evidence beyond metadata assertions: actual renderer/action/frame observations at desktop/mobile and 320px fault/preference scenarios. No browser was operated by this reviewer, and these receipts do not replace independent visual fidelity or performance review.
- Header placement removes the fixed mobile invitation from the purchase-content area; the supplied 320px receipt records a static 44×46 control contained inside the 64px header with no overflow. Broader route/state clearance remains the browser QA owner's evidence.
- No live checkout/payment, authenticated account, production/staging deployment, or unrepresented native immersive route is certified by this source gate.

## Responsive hero decode follow-up

**Source review and focused regressions PASS.** The final hero controller waits for the actual responsive image's `decode()` promise before assigning video sources/loading/playing, then sets `video.poster` from the selected image's `currentSrc`. It adds no fixed audit delay. Decode rejection or an older browser without `decode()` preserves the existing motion path rather than permanently disabling it. Late completion still passes through visibility, document lifecycle, user-pause, reduced-motion and Save-Data gates. No new URL input or commerce operation was introduced.

With explicit root authorization, added only `tools/v2-runtime/test-visual-recovery.cjs` and executed its six passing tests against the full actual controller in a controlled DOM/media fixture. Coverage includes deferred source assignment until decode, latest responsive poster selection, offscreen waiting, initial/live preference changes, decode completion during pagehide and Back/Forward restoration, decode failure/unsupported fallback, pause during decode, and late play resolution after hiding. These are behavior/lifecycle tests, not a substitute for real image/video decode or paint/performance measurements.

The actual resolver evidence gap is now closed for the supplied export: independently ran `node tools/v2-runtime/check-approved-scroll-world-scenes.cjs --resolved .artifacts/v2-visual-recovery-20260905/resolved-scenes-final.json`. Result: **PASS_APPROVED_SCENE_SELECTION**, nine resolved scenes, 27 selected media assets, two supporting derivatives, `resolved_selection_checked: true`. Root owns the PHP export's capture; the reviewer independently validated that supplied output against the current manifest/assets. No browser, Lighthouse, build, PHP execution or runtime mutation was performed in this follow-up.

## Pixel-review integration repair recheck — 2f257f3d

The later founder directive authorizes bounded creative/engineering upgrades while preserving mandatory approved systems. Reviewed the card-frame transparency/proportion repair, external scene-control markup, nearest-scene controller lookup, numeric escaped ratio, and desktop rail sizing without browser/build execution.

The card adjustments are scoped to the approved statue-frame treatment and retain existing product pixels and native commerce. Moving the scene button after the figure preserves DOM focus order, its existing `aria-controls` target, and native product links. The controller now finds the button through the nearest scene article, matching the revised markup. The aspect ratio is derived from positive-clamped numeric dimensions and escaped before inline CSS output; it introduces no arbitrary style input.

Two actionable findings sent to root:

1. **Accessibility:** `visibility: hidden` on the ready poster removes its alt description from the accessibility tree, while the replacement video is `aria-hidden="true"`. Use visual-only `opacity: 0` or retain an equivalent accessible description so the visual edge-art repair does not discard scene meaning.
2. **Home portrait sizing:** the desktop aspect-ratio flex-basis selector targets direct scene articles, but Home's rail children are `.sr2-recovery-home-world` wrappers. Home's BR-COMMERCE-1 portrait (960×1707) therefore keeps the prior 94%-width sizing. To apply the intended height cap to Home, propagate the approved ratio onto that wrapper and target it too. Collection-page direct articles already match.

The six earlier hero-decode tests are not tests of the separately modified collection-film controller; actual control discovery and screenshot geometry remain browser-QA evidence.

**Targeted recheck: both findings resolved; source gate PASS.** Ready posters now use `opacity: 0`, retaining their accessible alt description and existing layout while removing duplicate visible art. Home's direct `.sr2-recovery-home-world` wrapper now receives the same escaped numeric ratio from its selected approved scene, and the desktop flex-basis selector explicitly includes that wrapper. Existing collection-page articles remain covered. No further source defect found in these targeted repairs; root owns recompilation and final button/geometry browser verification. No additional tests, browser or build were run by this reviewer for this recheck.
