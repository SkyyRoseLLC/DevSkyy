# SkyyRose — Python Pipeline Dependencies
# Install: pip3 install -r requirements.txt
#
# NUMPY COMPATIBILITY NOTE
# rembg → pymatting uses numba 0.66, which requires numpy>=1.22,<2.5.
# Keep the NumPy pin inside that range; verify resolution, imports, and
# real Numba/ONNX CPU operations together before upgrading this set.

# ── Core background removal ────────────────────────────────────────────────
rembg==2.0.77
pymatting==1.1.15
numba==0.66.0
llvmlite==0.48.0

# ── Image processing ───────────────────────────────────────────────────────
Pillow==12.3.0
scipy==1.18.0
scikit-image==0.26.0

# ── AI model inference ─────────────────────────────────────────────────────
onnxruntime==1.29.0

# ── File system watcher ────────────────────────────────────────────────────
watchdog==6.0.0

# ── PINNED — compatible with numba 0.66 ────────────────────────────────
numpy==2.4.6
