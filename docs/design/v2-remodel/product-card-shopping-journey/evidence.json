{
  "candidateId": "a7fbceec7b313a99a989bff7a19745b0008e73a2+isolated-card-timeout-recovery",
  "claimClass": "LOCAL_SOURCE_GATES_PASSED_FIXTURE_BROWSER_EVIDENCE",
  "artifacts": [
    { "id": "candidate-manifest", "path": "candidate-manifest.json", "purpose": "current worktree identity and scoped change inventory" },
    { "id": "preview", "path": "preview.html", "purpose": "stable-ID visual handoff" },
    { "id": "prototype", "path": "_prototype/index.html", "purpose": "throwaway three-variant comparison" },
    { "id": "e2e-template", "path": "e2e-spec-template.json", "purpose": "read-only and fixture-gated browser proof" }
  ],
  "constraintsVerifiedInSource": [
    "verified-card media is fail-closed",
    "product image crop is contain inside the active stone statue frame",
    "server-derived product mode gates loop quick add",
    "card rail uses the approved SkyyRose still graphic and ceremonial Cinzel collection lockup rather than a numeric chapter",
    "hover detail is sourced only from WooCommerce short description and currently purchasable, in-stock variation attributes",
    "reduced-motion and coarse-pointer reel fallback is present",
    "the top graphic and miniature Cinzel collection label form a single centered masthead, while the statue lockup, commerce, hover disclosure, and action stack share the media center axis without page-level horizontal overflow",
    "simple-product quick add clears busy state only for its own initiating card and restores a localized truthful retry message after eight seconds without a Woo confirmation",
    "touch users receive a native disclosure populated from the same server-derived card story and available-variant summary"
  ],
  "fixtureBrowserEvidence": {
    "route": "tools/v2-theme-preview.php?route=shop",
    "viewports": [1440, 390],
    "states": ["desktop hover", "keyboard focus with prefers-reduced-motion", "mobile coarse pointer", "View piece to PDP"],
    "observed": ["four visible SkyyRose graphic card marks", "four visible Cinzel collection lockups", "the top mark and collection label are a single two-row masthead: both measure 0px horizontal delta from the media axis at 1440 and 390, with the mark above the label", "the collection label uses the Cinzel token at 10.3px desktop and 9.28px mobile", "desktop disclosure opacity transitions from 0 to 1", "all centered card elements measure 0px horizontal delta from the media axis at 1440 and 390", "the page scroll width equals the viewport at 1440 and 390", "the 390px mobile action stack is one column with a 46px minimum action height", "at 390, Piece details is visible with a 46px native summary control, opens to the server-derived story, and leaves the static reel visible", "a fixture-safe jQuery lifecycle shim proves an eight-second timeout clears only alpha to a localized retryable error while a same-SKU beta card remains busy and independently reaches localized success", "reduced-motion and coarse pointer keep the verified-media reel static", "View piece reaches a rendered sr2-pdp-product without fatal-error text after the fixture hook compatibility repair"],
    "limitation": "The local fixture intentionally does not implement get_available_variations(), so it does not establish a real product's variant list. Variant values require a fresh real WooCommerce staging read after deployment approval."
  },
  "pendingEvidence": [
    "fresh browser captures at 390, 768, and 1440 against this candidate, including fine-pointer hover/focus and reduced-motion",
    "read-only staging run using a real collection URL",
    "approved disposable fixture before any add-to-cart mutation test"
  ]
}
